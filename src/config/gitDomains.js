// Mapping from platform name (enum saved in OAuthCredential) to main domain
const gitDomains = {
  github: 'github.com',
  gitlab: 'gitlab.com',
  bitbucket: 'bitbucket.org',
  gitee: 'gitee.com'
};

module.exports = gitDomains;
